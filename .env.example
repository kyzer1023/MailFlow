# Optional local test labels. These are never consumed by the deployed app.
# Prefer interactive Microsoft sign-in and do not store passwords unless a
# controlled local browser test specifically requires them.
TEST_PRIMARY_STUDENT_EMAIL=
TEST_SECONDARY_STUDENT_EMAIL=

